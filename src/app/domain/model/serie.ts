export class Serie {

  constructor(weight: number, reps: number, rest: number) {
    this.weight = weight;
    this.reps   = reps;
    this.rest   = rest;
  }

  weight: number;
  rest: number;
  reps: number;
}
